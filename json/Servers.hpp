#pragma once

namespace openstack {
namespace json {

/** Provides a mechanism to translate a model::Servers instance to and from JSON
  */
class Servers {
public:
    Servers();
};



} // namespace openstack
} // namespace json
